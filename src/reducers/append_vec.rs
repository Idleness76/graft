#[expect(unused)]
use crate::{node::NodePartial, state::VersionedState};

// use super::Reducer;

// #[derive(Debug, PartialEq, Clone, Hash, Eq)]
// pub struct AppendVec;
// impl Reducer for AppendVec {
//     fn apply(&self, value: &mut VersionedState, update: &NodePartial) {
//         if !update.is_empty() {
//             value.extend(update);
//         }
//     }
// }
