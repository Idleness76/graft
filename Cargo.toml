[package]
name = "graft"
version = "0.1.0"
edition = "2021"
description = "Graph-driven, concurrent agent workflow framework with versioned state, deterministic barrier merges, and rich diagnostics."
license = "MIT"
repository = "https://github.com/Idleness76/graft"
readme = "README.md"
keywords = ["graph","workflow","concurrency","agents","tracing"]
categories = ["concurrency","data-structures","algorithms"]
rust-version = "1.83"
homepage = "https://github.com/Idleness76/graft"
documentation = "https://docs.rs/graft"

[dev-dependencies]
proptest = "1"
tempfile = "3"

[dependencies]
# Error handling & diagnostics
miette = { version = "7.6", features = ["fancy"] }
thiserror = "2"

# Tracing & logging  
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "fmt", "ansi"] }
tracing-error = "0.2"

# Async runtime
tokio = { version = "1", features = ["rt-multi-thread", "macros", "sync", "time"] }
async-trait = "0.1"
futures-util = "0.3"

# Serialization
serde = { version = "1", features = ["derive"] }
serde_json = "1"

# Data structures & utilities
rustc-hash = "2.1"
flume = "0.11"

# Time & IDs
chrono = { version = "0.4", features = ["serde"] }
uuid = { version = "1", features = ["v4"] }
rand = "0.9"

# LLM framework (external dependency)
rig-core = "0.20"

# Database
sqlx = { version = "0.8", features = [
    "runtime-tokio-rustls",
    "sqlite", 
    "macros",
    "uuid",
    "chrono",
    "json"
] }

# Configuration
dotenvy = "0.15"

# Optional future section:
# [package.metadata.docs.rs]
# all-features = true
# rustdoc-args = ["--cfg", "docsrs"]

[features]
default = ["sqlite-migrations"]
sqlite-migrations = []